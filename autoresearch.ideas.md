- Consider filing an upstream Bun coverage issue (or adding a reusable repo helper) for raw LCOV rows that mark blank lines, punctuation-only structural lines, and multi-line template-literal content as uncovered executable lines. This ticket now compensates transparently in `scripts/audit-core-config-keychain-coverage.sh`, but a broader fix would let other audits reuse the same normalization semantics.
